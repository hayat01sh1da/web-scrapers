[tool.mypy]
python_version = "3.14"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
# selenium, pandas and PIL ship without (complete) type stubs; treat their
# imports as untyped rather than failing the type check.
ignore_missing_imports = true
